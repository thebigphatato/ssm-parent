<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.3</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>com.atguigu</groupId>
    <artifactId>springboot-01-demo</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>springboot-01-demo</name>
    <description>springboot-01-demo</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <scm>
        <connection/>
        <developerConnection/>
        <tag/>
        <url/>
    </scm>
    <properties>
        <java.version>17</java.version>
    </properties>
    <dependencies>
<!--    SpringBoot场景启动器：

            每个场景启动器都有一个基础依赖：spring-boot-starter：这是场景启动器的场景启动器

            spring-boot-starter-xxx
            把当前场景用的jar包都导入进来；也就是引入当前场景所需要的依赖

            官方写的：spring-boot-starter-*
            第三方的：*-spring-boot-starter

            spring-boot-starter-aop
            sping-boot-starter-data-jdbc
            spring-boot-starter-test
            spring-boot-starter-tomcat
            spring-boot-starter-json

        为什么在子模块的pom文件中不用写依赖的版本号？
        因为父项目已经写了
        一般情况下，spring-boot-starter-parent里面会导入相关依赖
        而它的父亲，也就是当前pom.xml的爷爷”spring-boot-dependencies里面会定义各个版本的信息“
        依赖管理：父项目不管理的依赖，导入的时候都需要写版本
            修改版本号：
                1、写上：maven是就近优先原则
                2、自己声明属性，子项目覆盖掉父项目的版本




    -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>annotationProcessor</scope>
        </dependency>
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
